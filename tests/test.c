#include "../src/get_next_line.h"
#include <fcntl.h>
#include <unistd.h>
#include <stdio.h>
#include <assert.h>
#include <string.h>

int main(int argc, char ** argv)
{
	char* filename = argv[1];
	printf("file: %s\n", filename);
	int fd = open();
	return (0);
}